# 🤗 HF Daily · 2026-07-18：抱抱脸今日热门辛辣点评

**日期**: 2026-07-18

---

数据来自 HuggingFace trendingScore ，每日 16:00 更新。
## 模型 Top 5### 1. zai-org/GLM-5.24071个点赞，53万下载，热度226——这数据一看就是被大厂宣发和社区自来水双重加持过的。GLM-5.2，名字暗示第五代、52B参数？实际tags里挂着 glm_moe_dsa ，明显是MoE（稀疏专家）架构，还贴了个arxiv论文链接。但问题是，这种级别的模型通常会在README里放一堆benchmark对比，然而我翻了一圈，除了“我们更强”的模糊表述，连个像样的消融实验都没看到。说它是真创新吧，MoE+对话已经是老套路；说它是套壳吧，人家至少自己训了个新权重。不过5.2这个版本号，让人怀疑是不是跟某手机系统学的——跳版本号是为了显得迭代快？
点赞/下载比高达0.76%，说明大部分人是“下载了但懒得点赞”，这模型恐怕是拿来跑个demo就删了。
### 2. prism-ml/Bonsai-27B-mlx-1bit1-bit量化，27B参数，mlx框架，主打on-device——这三个词放一起，简直就是移动端推理的“皇帝新衣”。1-bit量化意味着每个参数只占1比特，27B模型压缩到约3.4GB，理论上能在手机跑。但实际效果呢？Qwen3.5基座被压成1-bit后，智商大概也从博士生退化到了小学生。tags里还写了cuda和metal，说明支持跨平台，但谁会在GPU上跑1-bit？这模型大概率是个学术实验，生产环境谁敢用？不过对于想在本地玩大模型的极客来说，至少它给了个“能跑”的幻觉，比空谈理论强。
### 3. Wan-AI/Wan-Dancer-14B“音乐到舞蹈”生成，14B参数，image-to-video pipeline——听起来像是AI版的《舞力全开》。但仔细看tags，居然把“video generation”拼成了“video genration”，这README的校对水平堪忧。模型声称能从图像和音乐生成跳舞视频，但舞蹈动作的连贯性、节奏对齐都是老大难问题。14B参数不算小，但视频生成领域已经被Sora、CogVideo等卷到几十B甚至百B级，这个模型恐怕只能生成一些重复的摆手扭胯动作。不过，如果它能做到实时生成，那倒是个有趣的玩具，可惜从下载量（2185）看，大家还在观望。
### 4. Cactus-Compute/needleJAX框架、encoder-decoder、function-calling、edge on-device——这组合让人眼前一亮，但仔细一查，pipeline_tag是空的，连个基本任务都没标。名字叫“needle”，意思是“在数据堆里找针”？实际是个轻量级工具调用模型？tags里没有模型大小信息，也没有基座来源，大概率是蒸馏或微调自某个小模型。功能上强调tool-use和function-calling，但这类模型已经被GPT-4 function call和Claude tools碾压得渣都不剩。唯一亮点是JAX训练，对Google生态用户友好，但下载量才874，点赞257，说明关注的人多，真敢用的少。
### 5. InternScience/Agents-A1Qwen3.5-MoE基座，多模态视觉语言模型，主打agentic能力——这个名字起得挺有野心，“A1”暗示是Agent第一代。572点赞，3.4万下载，热度102，数据看着还行。但仔细看tags， image-text-to-text 意味着它只能看图+文本输出文本，并不能直接操作环境，所谓的“agentic”恐怕只是能理解指令并给出文字建议，离真正的自主Agent还差十万八千里。MoE架构倒是省参数，但Qwen3.5本身在中文多模态上表现不错，这个微调版可能在某些Agent benchmark上刷了分，但实际部署时，幻觉和长尾指令失败率估计感人。
模型名字里带“Agent”的，十个有九个是蹭热度，剩下一个还在实验室里没毕业。
## 数据集 Top 3### 1. markov-ai/gaming-500-hours名字直白：500小时的游戏数据。但tags里 size_categories:n<1K 表示样本数不到1000，这就很有意思了——500小时的游戏视频被切成了不到1000个片段？每个片段平均半小时？这数据集的颗粒度太粗了，训练游戏AI根本不够用。更奇怪的是下载量2.9万，点赞179，说明很多人下载了但发现质量不高？或许是用来做游戏内容理解（比如识别场景、动作）的预训练数据，但只有500小时，还不如自己录几把《我的世界》。除非这500小时都是高难度职业选手对局，否则价值有限。
### 2. schema-harness/arc-agi-3-schema-tracesARC-AGI（抽象推理挑战）的第三版schema trace数据，下载量0，点赞17——典型的小众硬核数据集。ARC-AGI本身就是AI推理的“高考题”，这个数据集提供了解题过程中的schema推导轨迹，对于训练能一步步推理的模型很有价值。但问题在于，只有不到1000个样本，而且格式是CSV，连个标准的JSON都没有。想要用它训练出能解ARC的模型，恐怕需要配合大量数据增强。0下载说明大家还在观望，毕竟ARC-AGI的官方数据已经够用了。
### 3. nvidia/Open-SWE-TracesNVIDIA出品，100K到1M规模，parquet格式，标注了CC-BY-4.0许可。Open-SWE-Traces，听名字像是软件工程（SWE）领域的trace数据，比如代码执行轨迹、调用栈等。这类数据对训练代码Agent、自动化调试模型非常有用。NVIDIA敢开源，说明质量有保证，而且100K+样本足够训练一个不错的基座。点赞56，下载8013，比例不错，说明社区认可度高。唯一遗憾是热度才15，可能是被前面几个模型刷屏了。如果你是做SWE-agent的，这个数据集值得细看。
## Spaces Top 1### 1. smolagents/hf-realtime-voice405点赞，热度51，docker部署——这名字“smolagents”暗示是小模型驱动的Agent？实时语音Space，大概率是语音识别+对话+语音合成的pipeline。但tags里只有docker和region:us，没有模型信息，也没有demo预览图。点进去估计是个黑盒子，只能对着麦克风说话然后听回复。这种Space最大的问题是延迟：docker部署在美国，国内用户延迟高，实时性打折。另外，如果底层用的是Whisper+GPT+XTTS这种经典组合，那也没什么新意。不过作为展示“实时语音Agent”概念的demo，足够在朋友圈装逼了。
Spaces热度高不代表好用，很多就是PPT式的演示，点两下就卡死。
